# Render Blueprint for the VetConnect Ekiti API.
# Deploy: create a Blueprint instance from this repo on Render.
services:
  - type: web
    name: vetconnect-ekiti-api
    runtime: node
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: PGSSL
        value: "true"
      - key: DATABASE_URL
        sync: false      # set from the Supabase connection string
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        sync: false      # set to the Vercel frontend URL
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: MAIL_FROM
        value: "VetConnect Ekiti <no-reply@vetconnect.ng>"
